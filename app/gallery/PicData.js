export default function PicData(id, picture, camera, location, description) {
    this.id = id;
    this.picture = picture;
    this.camera = camera;
    this.location = location;
    this.description = description;
}
